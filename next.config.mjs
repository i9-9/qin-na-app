/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Allow images from the public directory with special characters
    unoptimized: false,
  },
};

export default nextConfig;
